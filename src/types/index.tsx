export interface TypeSubmit {
  id: string;
  isRemovable: boolean;
  isHex: boolean;
  nameHex: string;
  red: number;
  green: number;
  blue: number;
}
export interface TypeFormVal {
  text: string;
}
